#include <stdio.h>
#include <string.h>
#include <Windows.h>
#include <wincrypt.h>

void handle_command(const char* command, const char* data, char* response) {
    void handle_command(const char* command, const char* data, char* response);
}